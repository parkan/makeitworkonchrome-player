# HLS Generator Server Configuration

# Port (Railway will set this automatically)
PORT=3001

# Path to clips manifest JSON (should be uploaded to Railway or fetched from R2)
MANIFEST_PATH=./clips-manifest.json

# Base URL for R2-hosted clips (update with your R2 bucket URL)
# CLIPS_BASE_URL=https://your-r2-bucket.r2.dev/hls_clips/
