# === Production Configuration (Railway) ===
# These values are set via Railway environment variables
# DO NOT commit secrets to this file - this is for documentation only

# Node.js Server
PORT=3001
MANIFEST_PATH=./clips-manifest.json

# CLIPS_DIR is NOT needed in production (all clips served from R2)
# CLIPS_DIR=/work/hls_clips  # <-- NOT NEEDED

# Base URL for clips (Cloudflare R2)
# All clips are served from R2, not from local filesystem
R2_BASE_URL=https://bacteria.farm

# Python Script Configuration (not used in production)
# These are only needed when regenerating the manifest
# PHRASE_MAP=/work/foucault-phrase-map.json
# FIXED_TEXT=/work/lives.txt
